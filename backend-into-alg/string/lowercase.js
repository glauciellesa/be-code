/* II - lowercase
Write a function which receives a character in uppercase and prints it in lowercase. */

const printLowercase = (name) => {
  console.log(name.toLowerCase());
};

printLowercase("HELLO");
