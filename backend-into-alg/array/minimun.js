/* III - minimum
Write an algorithm which receives an array of integers and prints its minimum. */

const printMinimun = (arr) => {
  console.log(Math.min(...arr));
};

printMinimun([1, 2, 3, 87, 8, 20, 24, 4, 2]);
