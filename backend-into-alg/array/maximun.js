/* II - maximum
Write an algorithm which receives an array of integers and prints its maximum. */

const arrayInteger = (arr) => {
  console.log(Math.max(...arr));
};

arrayInteger([1, 2, 3, 87, 8, 20, 24, 4, 2]);
